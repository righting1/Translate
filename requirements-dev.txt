# Development and docs dependencies (keep production clean)
mkdocs>=1.5.0
pytest>=7.4.0
pytest-asyncio>=0.23.0
coverage>=7.5.0
# httpx is also used by tests; keep here to avoid polluting prod if not needed at runtime
httpx>=0.27.0